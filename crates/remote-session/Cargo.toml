[package]
name = "trex-remote-session"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
publish.workspace = true
rust-version.workspace = true

# The client-side remote-control session — the phone's Rust core, pure Rust with
# NO FFI so it is testable standalone (over the in-memory loopback, against the
# real `remote-host` dispatcher). Holds the app-signing identity, drives the
# Register/Connect/AuthProve handshake + one-shot RPCs over the `remote-proto`
# `Transport` seam, and (later slices) folds the `HostEvent` stream into a
# `ChatThread` + runs the reconnect state machine. `mobile-core` (uniffi) will
# wrap this; iroh becomes one `Transport` impl underneath it.
[dependencies]
trex-remote-proto = { workspace = true }
trex-agent-core = { workspace = true }
ed25519-dalek = { workspace = true }
rand = { workspace = true }
anyhow = { workspace = true }
thiserror = { workspace = true }
# Runtime-agnostic async plumbing for the demux pump: mpsc/oneshot channels, an
# async mutex to serialize RPCs, and `select`/`Either`. No tokio dependency — the
# pump is drivable by any executor (a futures executor in tests, tokio in prod).
futures = { workspace = true }
# The `Connector` dial seam is an async trait (a proc-macro, no runtime).
async-trait = { workspace = true }

[dev-dependencies]
# The full-loop integration test drives this client against the real host
# dispatcher over the in-memory loopback transport.
trex-remote-proto = { workspace = true, features = ["testing"] }
trex-remote-host = { workspace = true }
trex-agents = { workspace = true }
futures = { workspace = true }
serde_json = { workspace = true }
# The terminal seam hands back a tokio mpsc receiver, so the fake source in the
# terminal loop test needs one to feed it.
tokio = { workspace = true, features = ["sync", "rt"] }
async-trait = { workspace = true }
