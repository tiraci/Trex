[package]
name = "trex-editor"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
publish.workspace = true
rust-version.workspace = true

[dependencies]
trex-core = { workspace = true }
# CREATE_NO_WINDOW on LSP-server spawns (a console `rust-analyzer.exe` would
# otherwise pop an empty terminal window out of the GUI-subsystem build).
trex-no-window = { workspace = true }
# Autosave cadence (debounce/enabled) is read from `AutosaveSettings`, a GPUI
# global installed by the app. Settings is a leaf crate — no dependency cycle.
trex-settings = { workspace = true, features = ["gpui"] }

# GPUI shell + editor primitive. gpui-component already wraps tree-sitter
# (rust/ts/js/go/json/markdown/...) and exposes the `Input` widget plus
# the `HoverProvider` / `CompletionProvider` / `DefinitionProvider` traits
# Phase 5 needs. We're a thin layer: file -> InputState -> Input render +
# LspClient wiring the provider traits.
gpui = { workspace = true }
gpui-component = { workspace = true }

# LSP wire types. Pin matched to gpui-component's transitive `0.97` so the
# provider trait signatures (gpui::Task<Result<Option<lsp_types::Hover>>>)
# resolve to the same concrete type as ours — no conversion shim.
lsp-types = { workspace = true }
# url::Url::from_file_path percent-encodes paths into file:// URIs that
# round-trip against rust-analyzer's response URIs — the naive
# `format!("file://{}", path.display())` breaks for paths with spaces.
url = { workspace = true }
# Resolves a configured language-server command that is a bare name rather than
# a path. Replaces a hand-rolled PATH walk that keyed on the Unix executable
# bit — a concept Windows does not have, where runnability is PATHEXT instead.
which = { workspace = true }

# Async runtime for the LSP child + stdio framing. Tokio is already alive
# inside gpui callbacks (see apps/desktop/src/main.rs:47-51 rt.enter guard).
tokio = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }

anyhow = { workspace = true }
tracing = { workspace = true }

# Renders ```mermaid fences in the markdown preview to SVG, headlessly. See
# the workspace manifest for the pin rationale.
merman = { workspace = true }

# PDF preview: parses the document and rasterizes one page at a time into a
# GPUI `RenderImage`. `image` is the same 0.25 line gpui uses, so the frame
# type handed to `RenderImage::new` is gpui's own.
hayro = { workspace = true }
image = { workspace = true }

# Phase 5 step 3 — file tree backend. `notify` is re-exported through
# `notify_debouncer_full::notify` so we don't list it twice in the graph.
ignore                = { workspace = true }
notify-debouncer-full = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }
# `#[gpui::test]` + `TestAppContext` for the headless view tests (PDF open /
# page step / off-thread render). Same feature the desktop crate enables.
gpui = { workspace = true, features = ["test-support"] }
